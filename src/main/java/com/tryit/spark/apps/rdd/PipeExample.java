package com.tryit.spark.apps.rdd;

/**
 * Created by agebriel on 12/13/16.
 */

/*
*   pipe(command, [envVars])
*   Pipe each partition of the RDD through a shell command,
*   e.g. a Perl or bash script. RDD elements are written to
*   the process's stdin and lines output to its stdout are
*   returned as an RDD of strings.
 */
public class PipeExample
{
}
